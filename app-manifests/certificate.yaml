apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: frontend-tls
  namespace: default
spec:
  secretName: frontend-tls-secret
  dnsNames:
    - your-domain
    - api.your-domain
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
